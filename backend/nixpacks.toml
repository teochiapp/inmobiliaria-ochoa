[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
    "npm ci --include=optional",
    "npm rebuild @swc/core"
]

[phases.build]
cmds = ["echo 'Skipping admin panel build - API only deployment'"]

[start]
cmd = "npm run develop"

[variables]
NODE_ENV = "production"
PORT = "1337"
