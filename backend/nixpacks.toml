[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
    "npm ci",
    "npm rebuild @swc/core",
    "npm rebuild sharp"
]

[phases.build]
cmds = ["echo 'Skipping admin panel build - API only deployment'"]

[start]
cmd = "npm run develop"

[variables]
NODE_ENV = "production"
PORT = "1337"
